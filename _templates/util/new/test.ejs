---
to: __tests__/utils/<%= name %>.test.ts
---
import { <%= name %> } from '../../src/utils/<%= name %>';

describe('no arguments', () => {
  test('returns something expected', () => {
    expect(true).toBe(false);
  });
});

